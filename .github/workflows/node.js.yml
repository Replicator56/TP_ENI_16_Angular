name: Angular CI/CD Secure with Firefox

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      # 1️⃣ Checkout du code
      - uses: actions/checkout@v4

      # 2️⃣ Setup Node.js avec cache npm
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      # 3️⃣ Nettoyage des anciennes dépendances obsolètes
      - name: Remove old obsolete packages
        run: |
          npm uninstall build jxLoader uglify-js || true

      # 4️⃣ Installer les dépendances sécurisées
      - name: Install dependencies
        run: npm ci

      # 5️⃣ Installer Angular CLI localement si nécessaire
      - name: Install Angular CLI
        run: npm install @angular/cli --save-dev

      # 6️⃣ Installer le launcher Firefox pour Karma
      - name: Install Karma Firefox Launcher
        run: npm install --save-dev karma-firefox-launcher

      # 7️⃣ Installer Firefox dans le runner
      - name: Install Firefox
        run: sudo apt-get update && sudo apt-get install -y firefox

      # 8️⃣ Vérifier les vulnérabilités restantes
      - name: Audit npm packages
        run: npm audit --audit-level=high

      # 9️⃣ Build Angular pour production
      - name: Build Angular project
        run: npx ng build --configuration production

      # 🔟 Lancer les tests sur Firefox
      - name: Run tests
        run: npx ng test --watch=fals
